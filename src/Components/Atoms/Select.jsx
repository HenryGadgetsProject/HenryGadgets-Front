import styled from 'styled-components'

const Select = styled.select`
    border          : .14em solid var(--default-primary);
    border-radius   : .3em;
    margin          : .5em 0;
    outline         : none;
    padding         : .8em;
`

export default Select
